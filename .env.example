# Copy to `.env` and fill in. Never commit the real .env.

# Roboflow (CarDD dataset download) — see stage1_detection/dataset.py
ROBOFLOW_API_KEY=

# Weights & Biases (experiment tracking)
WANDB_API_KEY=
WANDB_PROJECT=vehicle-damage-detection

# Paths (optional overrides; defaults are derived from project root)
# DATA_ROOT=./data
